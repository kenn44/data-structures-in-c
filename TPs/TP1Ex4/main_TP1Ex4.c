#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char *argv[]) 
{
	int tab[5] = {0}, n = 5, i = 0, s = 0;
	printf("Entrer le nombre d'elements du tableau:\n");
	scanf("%d", &n);
	//oubli
	printf("Saisir les elements du tableau(entiers)\n");
	if(i = 0, i < n, i++)
	{
		scanf("%d", &s);
		tab[i] = s;
	}
	printf("Voici les elements du tableau\n");
	if(i = 0, i < n, i++)
	{
		
		printf("%d", tab[i]);
	}
	
	return 0;
}
